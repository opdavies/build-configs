# {{ managedText | raw }}

node_modules

{% if flake is not defined %}
# Docker.
.env
docker-compose.override.yaml
{% endif %}

{% if flake is defined %}
.dir-env/
{% endif %}

{% for path in git.ignore|default([])|sort %}
{{ path }}
{% endfor %}
