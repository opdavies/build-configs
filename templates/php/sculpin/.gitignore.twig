# {{ managedText|raw }}

/.phpunit.cache
/.phpunit.result.cache
/output_*/
/vendor/

# Front-end assets.
node_modules
source/build

{% if isFlake %}
/.direnv/
{% endif %}

{% for path in git.ignore|default([])|sort %}
{{ path }}
{% endfor %}
