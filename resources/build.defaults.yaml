---
drupal:
  docroot: web
  simpletest:
    db: sqlite://localhost//dev/shm/test.sqlite

experimental:
  runStaticAnalysisOnTests: false

php:
  ini:
    memory_limit: 128M
    post_max_size: 2M
    upload_max_filesize: 2M

project_root: /app
